import React from 'react';

const NotFound = (props) => (
  <ul>
    <li className="not-found">
      <h3>No Results</h3>
      <p>There were no matches for that search query</p>
    </li>
  </ul>
);

export default NotFound;